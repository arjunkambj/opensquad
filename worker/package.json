{
  "name": "opensquad-worker",
  "private": true,
  "version": "0.1.0",
  "description": "OpenSquad Box-side worker spike: ASCII Box lifecycle adapter + Codex App Server stdio client (P03).",
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "spike": "node dist/spike.js",
    "generate:protocol": "codex app-server generate-ts --out src/generated/codex && codex app-server generate-json-schema --out protocol"
  },
  "dependencies": {
    "@asciidev/box-sdk": "0.0.34"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "~7.0.2"
  }
}
