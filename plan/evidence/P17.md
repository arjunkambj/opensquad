# P17 — Agency trial, corrections and video (preparation lane)

Date: 2026-09-16. Executor: subagent-P17.
Base revision: `4ac81cb` ("chore(plan): assign P11 gate, P19, P13 inbox,
P21/P09, P16, P17 lanes"), branch `main`.
Worktree: `../opensquad-worktrees/P17-feedback`, branch
`opensquad/P17-feedback`.

## Scope exercised

**Preparation only.** This lane produced the P17 materials: the invitation
draft, the moderated trial script, the feedback intake template, the V21
shot list with its fallback clip plan, the social post draft, and the
submission field draft. **No message was sent, no post was published, no
person was contacted, no deployment or provider call was made, and no Convex
command ran.** Nothing in this lane claims user validation — no participant
has been invited yet, so `plan/feedback/` contains templates and drafts, not
observations.

## Changed files

All new; no existing file was modified.

| Path | Content |
|---|---|
| `plan/feedback/README.md` | Pack index, status table and the consent/sanitization rules |
| `plan/feedback/invitation.md` | One concise invite for a single real agency user + a send checklist |
| `plan/feedback/trial-script.md` | 20-minute moderated run sheet: lead search/research, CRM ownership and status, reviewed outreach, inbox reply, proposed/confirmed booking — with pre-flight checklist, verbatim consent script, observer rules and contingencies |
| `plan/feedback/intake-template.md` | Per-session log: consent fields, observation → severity → proposed feature/fix → owning task card mapping, plus the paste-ready proposed card block format |
| `plan/feedback/video-shot-list.md` | Six shots on V21's 0:00–2:50 timeline with 10s margin, pre-record checklist, and the labeled fallback clip for provider latency |
| `plan/feedback/social-post.md` | X (≤280 chars) and LinkedIn drafts tagging @convex @OpenAI @firecrawl @agentmail, with a claims checklist to re-verify on release day |
| `plan/evidence/submission.md` | Submission packet draft: re-checked deadline, required items, field values, description draft and pre-submit checklist |

## Contract changes

None. No schema, validator, sidebar, route or lockfile change; no shared
reserved file was touched. `convex/`, `src/` and `worker/` are untouched.

## Checks run

- `pnpm lint` — clean, 0 warnings / 0 errors (919 files).
- `pnpm build` — passes: `tsc -b` clean, `vite build` clean with the
  pre-existing >500 kB chunk advisory, worker `tsc -p tsconfig.json` clean.
  First run failed only because `worker/` is not a pnpm workspace member and
  its `node_modules` had not been installed by the root
  `pnpm install --frozen-lockfile`; `pnpm -C worker install` (auto-run by
  its build) resolved it. Not a code issue.
- `node scripts/plan.mjs show P17` — the tracker reports expected
  dependency warnings (P17 `in_progress` while P16 is still `in_progress`,
  and sibling lanes likewise); this is the integrator's assignment state,
  not a tree defect.

## Manual outcomes

None yet — by design. The trial, the recording and the submission all
require the decisions below and, for the trial, a working public/deployment
URL.

## Dependency reality at write time

On this lane's base (`4ac81cb`): `/leads` and `/inbox` routes do not exist
yet (P13 in flight); `convex/bookings.ts` / `convex/leadEvents.ts` do not
exist yet (P19 in flight); the P11 reply path is in progress; P16 hosting is
in progress, so there is no public URL to invite anyone to. The trial
script's pre-flight checklist verifies each of these on the actual
deployment before the session is scheduled — a missing surface is recorded
as feedback, never simulated.

## Blockers and decisions awaiting the user

1. **Invitation send decision.** Who to invite (one real agency user), from
   which sender address, at what time, and whether to send at all. Needed
   because sending email to a person requires an address and authorization
   the lane does not hold; `plan/feedback/invitation.md` is ready to
   personalize once answered.
2. **Waitlist/feedback form on the marketing page — yes or no.** If yes, on
   resume this lane builds it as a Convex mutation with backend rate
   limiting and no third-party form service; if no, the intake stays a
   private log.
3. **Recording and publishing authorization** for the V21 video and the
   social post once the release exists.

## Honest-status note for the integrator

P17's card gate — "usable video under three minutes, honest feedback
evidence (or clearly documented external blocker), release still working
after fixes" — is **not met** by this lane. What is met: every preparation
artifact the card names exists, is labeled draft, and is ready to execute
the moment a participant, a deployment URL and the send/publish
authorizations arrive. If the trial cannot run before the deadline, the
"clearly documented external blocker" clause applies and this file plus the
session-less intake template are the evidence.
