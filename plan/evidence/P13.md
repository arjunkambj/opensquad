# P13 — Connect the Leads CRM, booking and supervision UI

Status: **done** (accepted by integrator in two parts).

This card was executed by two lanes; the detailed evidence lives in:

- `plan/evidence/P13-inbox.md` — part A: `/inbox` + `/inbox/$conversationId`
  (filters, safe thread render, notes, takeover/resume/close, unassigned-queue
  association with explicit resume), the shared Decisions implementation
  (queue/detail split, exact preview, revision conflicts, request-changes,
  rejection reasons — reused by inbox and mission detail), `?section=` settings
  with runtime/provider controls, sidebar entries. Contract-verified against
  `dev:flexible-grasshopper-949` with a seeded fixture workspace.
  Accepted at merge `4e39904`.
- `plan/evidence/P13-leads.md` — part B: real `/leads` pipeline/due list, lead
  detail tabs, full booking lifecycle UI with shared-Decisions draft linkage,
  `/leads` as signed-in home (`afterSignIn`/`afterSignUp` + sanitized
  `after_auth_return_to`), plus bounded backend reads added where no index
  could answer the UI. Accepted at merge `1e55592`.

Deferred (recorded in the part files): signed-in browser walks of J3/J4/J6 and
the /leads + booking flows await an operator session; `delivery_uncertain`
panel render remains untested against live data (exists only after a real send
attempt); seeded threads show empty inbound halves because inbound bodies are
written only by the signed webhook.
