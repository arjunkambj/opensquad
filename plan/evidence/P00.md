# P00 — Planning handoff evidence

Completed: 13 September 2026. Application baseline: `988fb16`.

## Delivered

- Self-contained `plan/` specification and 20 task cards, including core lead
  CRM/booking and optional scheduling. Only P00 is completed by this handoff.
- App authentication, authorization, schema/indexes, backend contracts, workflow
  and external worker protocol with exact approval/send/booking boundaries.
- ASCII lifecycle and Codex managed login/run runbooks with provider feasibility
  probes. Chosen research route is the Firecrawl component; Apollo uses a scoped
  gateway; AgentMail sending uses the reviewed narrow adapter design.
- Root `AGENTS.md`, repository-local `opensquad-build` skill, retained hackathon
  skill, root README entry and `pnpm plan` commands.
- 24 manual acceptance scenarios, dependency/owner/file/skill handoffs, public
  demo and release/submission procedure. Source documents in ignored `docs/`
  were read; the original ignore change was preserved.

## Verification performed

| Check | Observed result |
|---|---|
| `pnpm plan check` | 20 cards; no missing linked files; no dependency cycles; card dependencies agree with tracker |
| `pnpm plan status`, `show P03`, `show P19` | Correct task state, prerequisites, file/skill scope, complete card and execution prompt |
| `node scripts/plan.mjs show P99` | Expected nonzero exit with unknown-task explanation |
| `node --check scripts/plan.mjs` | Syntax accepted |
| Skill creator validator, new build skill | Valid frontmatter/name/references scaffold |
| Skill creator validator, existing hackathon skill | Valid |
| `pnpm lint` | Exit 0; existing `react(set-state-in-effect)` warning in `src/hooks/use-mobile.ts` |
| `pnpm build` | Exit 0; Vite build completed with existing large-chunk advisory |
| `git diff --check` | No whitespace errors |
| Whole `hackathon.md` redaction scan | No address-shaped text, private-key markers or checked credential patterns |

Independent reviews checked document precedence, required product/UX behavior,
provider contracts and the executable handoff. The final plan assigns early
provider-only probes separately from later application acceptance; independent
prospect waits, runtime/control liveness, provider receipts and booking state
changes have explicit persistence contracts.

No test files were written. No provider login, paid resource, live model run,
external email, production deployment, social post or submission was performed.
Documentation/package checks do not prove hosted integration success; P03–P05
and later application gates remain pending. The build verifies the existing
frontend and helper changes, not an implemented CRM.

## Parallel execution refinement

Removed all 17 development effort estimates and the internal dated build
schedule/investigation cutoffs at the user's request. Added `plan/worktrees.md`
and aligned the build skill, root instructions and printed task prompts with
parallel task worktrees, one canonical integrator, isolated development backends
and dependency-ordered review/merge. Provider quotas, runtime deadlines and the
official event cutoff remain product/operational requirements.

## Pre-commit review — 13 September 2026

Reviewed the dirty handoff against the frontend baseline and obtained an
independent read-only contract review. Corrected three planning gaps: unassigned
mail requires human lead association and explicit resume; unresolved sends block
new revisions unless a recorded, single-use replacement decision permits them;
company-name search now has a P19 index/query contract and P13 UI ownership.
Aligned schema/API declarations, task cards and V16/V18/V23 manual scenarios.
These are specifications; no mail, CRM or search integration was implemented.

The navigator now rejects `in_progress` tasks with unfinished prerequisites and
task cards missing dependency declarations. In disposable copies of the handoff,
starting P02 while P01 was pending and removing P02's dependency declaration both
returned exit 1 with specific errors. `show P03` and `show P19` returned their
cards; `show P99` returned the expected unknown-task error. No test files were
written and the disposable copies were removed.

`pnpm plan check`, `pnpm plan next`, `pnpm plan status`, JavaScript syntax checking,
`pnpm lint` and `pnpm build` passed. Only P00 is done; P01 is the next ready task.
The existing hook warning and large-bundle advisory remain unchanged. The final
independent review found no remaining blockers in the corrected contracts.

Rechecked the [official event requirements](https://www.convex.dev/hackathons/all-gas),
[Codex managed login protocol](https://learn.chatgpt.com/docs/app-server),
[headless login prerequisites](https://learn.chatgpt.com/docs/auth),
[ASCII create/lifecycle contract](https://docs.ascii.dev/box/api/v1),
[Apollo connection prerequisites](https://docs.apollo.io/docs/apollo-mcp) and
[Convex search contract](https://docs.convex.dev/search/text-search).
Local `codex --version` remains `0.154.0` and its help confirms the documented
stdio/schema tooling. No hosted login or provider acceptance was exercised.
