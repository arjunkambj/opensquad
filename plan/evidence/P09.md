# P09 — Apollo discovery and contact enrichment

Date opened: 2026-09-17. Executor: subagent-P21-runtime (lane 4).
Base revision: `8f6f0f4`; working branch `opensquad/P21-runtime`,
worktree `../opensquad-worktrees/P21-runtime`.
Task revision: `plan/tasks.json` P09 — `status: in_progress`,
`dependsOn: [P04, P21]`, files `convex/integrations/apollo.ts`,
`convex/prospects.ts`, `convex/workflows/sales.ts`, `plan/evidence/P09.md`.

## Status: prepared, blocked on the owner's OAuth grant

No Apollo provider call has ever been made. No `tools/list` has been
observed, so no tool names are pinned and none are guessed anywhere in the
codebase — `convex/integrations/apollo.ts` does not exist yet on purpose.

### What exists and is already verified (P21-owned halves)

- **Discovery seam** — `convex/workflows/sales.ts:sourceProspects` carries
  the documented plug point (`internal.integrations.apollo.searchCompanies`
  → `internal.prospects.importCampaignProspects`). The importer, the
  campaign `leadLimit` ceiling, canonical-domain dedupe and provenance merge
  are shipped and were exercised live on `dev:flexible-grasshopper-949`
  (see plan/evidence/P21.md).
- **Enrichment seam** — `checkContact` documents the plug point
  (`internal.integrations.apollo.enrichContact` →
  `internal.prospects.applyContactEnrichment`). The applier already refuses
  unless the lead is `qualified` AND Convex has accepted evidence for it —
  G2 item 5 made mechanical.
- **Capability vocabulary** — `apollo.company_search` /
  `apollo.contact_enrichment` are already in `CAPABILITY_IDS`,
  `vCapabilityId` and `HOST_CAPABILITY_POLICY` (`convex/lib/validators.ts`),
  and the `discover`/`contact` role templates + output contracts exist in
  `convex/lib/roleTemplates.ts`. `usageBuckets` already carries the
  `apollo_enrichments` metric; nothing has ever taken a reservation.
- **Campaign fields** — `campaigns.enrichmentLimit` (0..25) and the
  `apollo` source kind are already schemaed.

### The probe that is committed and waiting (P04 machinery)

`worker/src/p04gate.ts` + `worker/src/boxmcp.ts`, run on the builder host:

1. `up` — one disposable `noEnv` Box, Node 24.21.0 + codex 0.154.0 +
   boxmcp bundle.
2. `auth` — `mcpServer/oauth/login` against `https://mcp.apollo.io/mcp`;
   the authorization URL is relayed to `worker/P04_AUTH_STATUS.json` the
   moment it appears; the owner completes Apollo sign-in in a browser and
   pastes the final loopback redirect URL into
   `worker/P04_CALLBACK_URL.txt`, which the driver ferries into the Box
   (codex 0.154.0 `mcp login` has no `--no-browser` flag — this relay is
   the headless path).
3. `list` — ground-truth direct MCP `tools/list` capture.
4. `call` — ONE bounded `mcpServer/tool/call` (one company search, then at
   most one enrichment).
5. `token` — presence-only `.credentials.json` inspection (existence and
   shape, never values — decides whether refresh is even needed).
6. `down` — delete, `GET /boxes` empty verification.

### Gate before implementation

Only after a real `tools/list` is captured will this lane:

- pin the exact permitted Apollo tool names (company search, person
  search, ONE business-email enrichment operation) and their schemas;
- write `convex/integrations/apollo.ts` as the bridge-side tool adapter —
  Convex-side calls only; no Apollo credential or raw MCP surface enters
  the Box, and no guessed tool name is ever hardcoded;
- wire discovery into `sourceProspects` and enrichment into `checkContact`,
  preserving caps, dedupe, provenance, the `apollo_enrichments`
  reservation path and `contact_needed`;
- keep `convex/prospects.ts` edits limited to `importCampaignProspects` and
  `applyContactEnrichment`, coordinating with P19 rather than redefining
  schema or a second lead store.

Blocked-categories contract (must hold at the gateway): email
creation/sending, enrollment, sequences, tasks, contact writes, personal-
email reveal, phone reveal, waterfall enrichment, and any unknown tool.

## Changed files this pass

None — preparation only. (`worker/src/p21box.ts` and the
`OPENSQUAD_BOX_TTL_SECONDS` override are P21 runtime prep, recorded in
plan/evidence/P21.md.)

## Contract changes

None yet.

## Checks run

`pnpm lint`, `pnpm build` (incl. `worker` build), `tsc -p
convex/tsconfig.json --noEmit`, `tsc -p worker/tsconfig.json --noEmit` —
all clean on `opensquad/P21-runtime` at `3e9d2dd`.

## Manual outcomes

None — no provider was contacted.

## Blockers

- **The owner's Apollo OAuth grant** — one browser sign-in on an Apollo
  account, plus the pasted post-redirect URL (loopback, cannot load — by
  design). Nothing Apollo-side can proceed without it.
- **P04 task state** — `plan/tasks.json` marks P04 `blocked`/`in_progress`;
  the integrator owns canonical status.
- **Apollo spend** — the at-most-one enrichment may consume an Apollo
  credit; authorization required.
- **Token refresh / partner-app registration** — unverified until the
  token inspection step runs; P04 evidence records both as open questions.
