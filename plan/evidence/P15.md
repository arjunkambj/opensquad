# P15 — Optional schedule or follow-up

Date: 2026-09-18. Status: **skipped**.

P15 is stretch and is not a release prerequisite (`plan/tasks.md`). Core
acceptance (research → outreach → reply → booking, plus the Convex Apollo
adapter) is on main. Remaining release work is owner publish authorization
(P16) and the agency trial (P17).

Neither optional shape was selected:

- Recurring discovery would add a Convex-owned cadence that starts
  `salesMissionWorkflow` with occurrence dedupe, DST handling, edit/pause/
  run-now.
- A suggested follow-up would bind conversation version to a new exact
  approval and become obsolete on reply/takeover/suppression/pause.

Implementing either now would not unblock P16 publish or P17. Audit
screenshots/PDFs remain deferred as the card already states.

Skipped with reason, not failed core.
